import numpy as np
from stratego import *
from gen_board import *

if __name__=='__main__':
	board            =  gen_random_init_board()
	board_1, board_2 = np.copy(board),np.copy(board)
	board_1          = 
	stratego         = Stratego(1,1)
	stratego.setup()